# Prism VS Code extension changelog

## 0.8.1 — 2026-07-26

- Fix daemon token persistence and authorized handshake (no sticky 401s).
- Add Graphify-like **Prism: Setup Workspace** / `prism setup` (index + assets + MCP + daemon).
- Fix CLI JSON for index/index-status, compile refusal unwrap, impact resolve, slice line/spans.
- Keybindings no longer collide with Command Palette (`cmd+alt+p` chords).

## 0.8.0 — 2026-07-26

- Initial P8 release: commands, evidence + graph panels, daemon/CLI transport, MCP auto-registration, AGENTS.md generation, actionable refusals.
