# Sample Doc

Intro paragraph mentioning `compile_context` and the `crates/prism-ir` crate.

## Setup

Run the setup, see [the guide](./guide.md) and [jump back](#sample-doc).

```
# not a heading inside a fence
also `not_a_mention` here
```

### Detail

Uses `NodeKind` and links to [external](https://example.com).

## Setup

Duplicate heading on purpose.
