//! Criterion benches land when CI perf gates are enabled (late P0 / early P1).
//!
//! Planned: `fingerprint_incremental` — edit one file in a fixture tree and
//! measure discover+hash skip path latency.
